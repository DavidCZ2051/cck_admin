const String url = "localhost";
const String appVersion = "1.0.0-DEV";
const bool debug = true; //debug variable
User user = User();

class User {
  String? firstName;
  String? lastName;
  String? token;
}
