const String url = "localhost";
const String appVersion = "DEV-1";
const bool debug = true; //debug variable
User user = User();

class User {
  String? firstName;
  String? lastName;
  String? token;
}
